pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                sh 'git --version'
                sh 'mkdir /mon123'
            }
        }
    }
}
