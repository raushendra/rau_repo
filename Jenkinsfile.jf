pipeline {
    agent { label 'master' }
    tools{
    maven "3.6.0"
    }
    
    stages {
        stage('build') {
            steps {
                sh 'git --version'
                sh 'maven --version'
                sh 'mkdir /mon123'
            }
        }
    }
}
