pipeline {
    agent { label 'Rau_testing' }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh 'mkdir /mon123'
            }
        }
    }
}
