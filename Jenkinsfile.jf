pipeline {
    agent { label 'master' }
    stages {
        stage('build') {
            steps {
                sh 'mvn --version'
                sh 'mkdir /mon123'
            }
        }
    }
}
